import React from 'react';
import CounterContainer from 'src/components/container/CounterContainer/index';

interface Props {}

const CounterPage = (props: Props) => {
	return <CounterContainer />;
};

export default CounterPage;
