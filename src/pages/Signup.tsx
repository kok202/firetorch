import React from 'react';
import EmailSignupContainer from 'src/components/container/EmailSignupContainer';

interface Props {}

const UserSignupPage = (props: Props) => {
	return <EmailSignupContainer />;
};

export default UserSignupPage;
