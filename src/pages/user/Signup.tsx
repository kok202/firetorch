import React from 'react';
import UserEmailSignupContainer from 'src/components/container/UserEmailSignupContainer';

interface Props {}

const UserSignupPage = (props: Props) => {
	return <UserEmailSignupContainer />;
};

export default UserSignupPage;
